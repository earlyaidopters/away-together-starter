# Overnight launch prompt: Travel Agency

Prepared 20 September 2026. Launch instructions for a future execution turn. No overnight process is running merely because this file exists.

Build the travel-agency experiment described in production/TRAVEL-AGENCY-END-TO-END-PLAN.md in this project. Read that file in full first, plus governing AGENTS.md and the canonical VIDEO-PACKAGE-BRIEF.md. Treat this prompt as an execution request only when Mark actually launches it. The earlier buying-signal prompt is archived and superseded.

Create a real locally runnable travel classifier from open pretrained weights, train it on independently constructed data, evaluate it against baselines and Jev on identical inputs, and deliver a visual travel-agency app with truthful run receipts, plots, captures and a filming package. Astra is the builder; the trained model must run without Astra at inference time. Do not quietly replace model training with a hosted API wrapper.

Default scope: holiday matching through classification of offer descriptions, booking terms and explicit traveller requirements. Use a four-friend focus view and a 12-client agency grid. The optional travel-document scene stays secondary. Follow any newer domain choice Mark gives before execution.

Use the source and Hugging Face pins in research/. Primary candidate: answerdotai/ModernBERT-base with a new candidate-scoring head. Run the prescribed local MPS training admission test before choosing a training route. Retain a pretrained classifier baseline and the frozen-encoder trained head; attempt top-layer fine-tuning only if measured speed fits the time budget. Follow the fallback hierarchy rather than debugging hardware all night.

Use Python 3.12 in a project-isolated environment. The inspected host is an M5 Max with 128 GB memory. Verify current availability and resource use. The existing private Jev credential location is in the plan and its auth check succeeded at planning time; read it privately and do not ask Mark to paste it again. Use the direct Jev API, not the public playground, for timed comparisons. No secret goes into files, browser assets or logs.

Implement a resumable stage controller, actual documented launch commands and a live model endpoint. Store all checkpoints, dataset/config hashes and predictions before building visual claims. The app must use the exported selected model. Build the interface while a single training job runs, without concurrent competing GPU jobs.

Construct scenario truth before text. Keep train, development, calibration, final test and challenge families separate. Record all synthetic and model-assisted label provenance. Do not use Jev outputs for training, reference labels, calibration or development. Freeze candidate weights, rubric, prompts and thresholds before the final comparison. Do not tune on final errors. Human review cannot be fabricated; if absent, label travel results synthetic/oracle-checked and assistant-reviewed.

Deliver separate benchmark lanes: travel usefulness; official TypeSafe workflow reproduction if exact accessible data/harness can be verified; independent LocalLLaMA/typed-decisions; and independent AbdelStark classification-pilot reproduction. These are different sources with different reference labels. A public chart is not a runnable benchmark. If official assets remain unavailable, say so and complete the independent lanes without misnaming them. Never substitute a published historical Jev score for the fresh matched Jev condition.

The public MCA changed after the previous project notes. Its remaining competitor-development clause requires an account-specific checkpoint before the Jev comparison arm. Complete independent work while this is unresolved, report Jev as not run if necessary, and do not contact anyone without authorization. Do not claim that the user's API access alone settles vendor terms.

On launch, set an absolute deadline and enforce the proposed defaults unless Mark overrides them: 10 hours total, last 90 minutes reserved for QA/reports, 3 hours training, 2 trained candidates, 12 GB model downloads, USD 10 estimated Jev cap with reservation margin, no rented GPU and no new paid service. Report spend uncertainty. Do not enable billing or automatic top-ups. Existing authorization to compare models does not authorize public deployment or distribution.

The hero is a real offer card moving through explicit requirements and triggering traveller reactions. Unknown evidence shows a question/review state. Numeric filters run in code. Changing group weights may reuse judgments; changing semantic requirements reruns inference. Live Run always runs fresh; Saved Run is labelled. Pause queues presentation events; Reset does not spend money or accept stale responses. Do not fabricate reaction timing, confidence, explanations, prices or availability.

Generate reports from raw JSONL results and SVG/PNG charts from report data. Show all failures and confidence intervals with correct independent units. Keep OCR or preprocessing, model inference, network time and animation time distinct. Compare local deployment and hosted deployment honestly, including assumed local costs.

Complete meaningful model, dataset, metric, policy and UI tests. Inspect real browser states at all plan viewports. Save screenshots of first/middle/final states. Follow the project's concept/production gates before the final script/filming package. Use actual outcomes, one title-thumbnail candidate, a recent voice reference, exact spoken guide blocks and a visually inspected guide PDF. Do not invent a model win to make the video work.

Preserve an intervention ledger. If any part cannot complete, finish all independent deliverables, save recovery checkpoints and identify the unmet dependency. Finish with START-HERE.md and a morning report containing app launch instructions, model identity, whether training and each evaluation lane actually ran, measured results, failures, raw artifacts, QA status and the next precise action. A beautiful app with no trained model is an incomplete result.
